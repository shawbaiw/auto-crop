# 03: Founder Decision — declaration and acceptance gating

**What to build:** When a completing agent makes a choice on a strategic business decision whose first value is the founder's to set, it declares that choice rather than letting its pick stand as direction. A deliverable carrying such an unresolved **Founder Decision** does not auto-accept and is not sent to manual CEO review — it records a Task Completion Event describing the choice and stops. Company state then shows the pending Founder Decision with its options and the agent's recommendation, and downstream work stays blocked until the founder decides (resolution itself is ticket 04).

Governing decision: `docs/adr/0017-ceo-office-surfaces-business-outcomes.md`. Spec: `.scratch/ceo-office-business-outcomes/spec.md`.

**Blocked by:** 01 (Deterministic Automatic Acceptance), 02 (Task Outcome Summary)

**Status:** done

- [x] `NextStepItemType` gains `founder_decision` (`packages/core` type + `nextStepItemTypeSchema`). The source task's Business Artifact stays kind `deliverable`.
- [x] `StrategicDecisionKind` type + `strategicDecisionKindSchema` added to core. Seed values: `target_market`, `product_direction`, `mvp_type`, `pricing_model`, `launch_target`.
- [x] Business Artifact parsing reads an optional `payload.open_decisions` array via `parseOpenDecisions` (`apps/server/src/runtime/founderDecision.ts`), called from `parseDeclaredBusinessArtifact` for `deliverable` / `final_report`. Each entry: `decisionKind`, `options` (>1, each `label` + `tradeoffs`), `recommendation` (must name an option), `rationale`.
- [x] Kept = known `decisionKind` + more than one well-formed option. Unknown `decisionKind` (or missing / non-string) → dropped silently. Malformed entry on a known `decisionKind` → structural validation failure (`invalid_schema`), error strings like `payload.open_decisions[0].options: ...`.
- [x] `recordTaskCompletionEvent` (`buildFounderDecisionItems`) turns each kept declaration into a `founder_decision` Next Step Item; `decisionKind` / ordered options (with `recommended` flag) / `rationale` / `blockedTaskIds` ride on `dependencyImpact.founderDecision` (nested so the blocked ids stay out of the generic dependency-impact scan). `ceoAttention.ts` `collectFounderDecisions` projects it into a first-class `FounderDecision` (core type near `HumanAction`, no zod): identity, source Task Completion Event, task, owning department, `decisionKind`, options, `rationale`, `status` (always `pending` here — resolution is issue 04), `resolvedOption` / `resolvedAt` (null), `blockedTaskIds`. `projectCeoAttention` returns `founderDecisions`.
- [x] `scheduler.ts` (inside the `evaluateAutomaticAcceptance` → `accept` branch, so a risk-pattern hit routes to manual review first): a deliverable with any kept `open_decisions` is parked in `review` status without an `automatic_acceptance` or `task_review` event; `recordTaskCompletionEvent` fires with the `founder_decision` items + `outcomeSummaryText` under a new `TaskCompletionOutcome` value `awaiting_founder_decision` (no `acceptanceProvenance`), then the dispatch returns. `createAttentionCandidates` treats `awaiting_founder_decision` like `accepted` (not an exception outcome) — Founder Decision attention is wired separately in 04/05. Downstream stays blocked because the artifact is never accepted (`dependencyReadiness` already blocks on a non-accepted upstream).
- [x] `buildAgentPrompt` gains a `## Open Decisions` block: declare any choice on the five Strategic Decision Kinds in `payload.open_decisions` with options / trade-offs / recommendation / rationale, and that such a choice is the founder's to make.
- [x] `buildCompanyState` serializes `founderDecisions` from `projectCeoAttention`. Dashboard `client.ts` gains `FounderDecisionSummary` and `CompanyStateResponse.founderDecisions`.
- [x] Tests: `schemas.test.ts` (Seam 4 — `founder_decision` type, every `StrategicDecisionKind`); `businessArtifact.test.ts` (drop unknown kind, fail malformed known kind, keep well-formed); `scheduler.test.ts` (gating: not auto-accepted / not routed to review, TCE carries the `founder_decision` item + `outcomeSummaryText` + `blockedTaskIds`, downstream stays blocked; unknown kind auto-accepts); `routes.test.ts` (company-state serializes `founderDecisions` with options, recommended flag, `pending` status, blocked task ids).

Notes for later issues:
- The task is parked in `review` status — the plan's sanctioned "review-equivalent" (A4: "move the task to `review`-equivalent … or a dedicated status-free projection"); no dedicated Task status was added. The dashboard's `getCeoPendingItems` / `CompanyOperations` still key off `task.status === "review"`, so a founder-decision-gated task currently shows in the (to-be-demoted) review queue until issue 05's queue demotion / review-detail restructure and the Outcomes view consume `founderDecisions` and pin/exclude it. Server-side behaviour is complete: not auto-accepted, no `task_review` runtime event, `outcome = awaiting_founder_decision`, downstream blocked.
- Issue 04's resolution runs the shared acceptance seam, which records its own `accepted` Task Completion Event — so a resolved task ends up with two TCEs (the `awaiting_founder_decision` one from here and the `accepted` one from resolution). The distinct outcomes keep them legible; issue 04/05 decide whether to supersede. `buildFounderDecisionItems` still fires for any `recordTaskCompletionEvent` call carrying a payload with `open_decisions`, so issue 04 will want to stop re-emitting the items once the decision is resolved.
- The `founder_decision` CEO Attention Rollup reason, the derived "waiting on decision" dependency display, and Founder Decision resolution (pick / return API, writing the value back, `acceptanceProvenance = "founder_decision"`, multi-decision acceptance) are issues 04 / 05. `TaskAcceptanceProvenance` is untouched here.
- `taskRefresh.ts` proof recovery routes a recovered deliverable straight to `review` (manual review) without consulting the founder-decision gate — a pre-existing recovery-path gap (it also bypasses Automatic Acceptance from issue 01). Out of scope here; the recovery path routes to a human, which is safe.
